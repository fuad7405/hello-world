name: Java CI with Maven

on:
  push:
    branches: [ master ]
    paths-ignore:
      - "hello-world-gradle/**"
      - "hello-world-war/**"
  pull_request:
    types:
      - opened
      - synchronize
    branches: [ master ]
    paths-ignore:
      - "hello-world-gradle/**"
      - "hello-world-war/**"

  workflow_dispatch:

jobs:
  publish-jar:
    uses: "fuad7405/github_workflows/.github/workflows/maven-8.yml@master"
    secrets: inherit
